<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zs.magicviews">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".nav.NavActivity"/>
        <activity android:name=".scala.ScalaActivity"/>
        <activity android:name=".motion.MotionLayoutActivity" />
        <activity android:name=".card.CardActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ruler.RulerActivity"
            android:label="ruler" />
        <activity
            android:name=".tag.TagActivity"
            android:label="tag" />
    </application>

</manifest>